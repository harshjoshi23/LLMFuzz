# Infineon DC optimizer firmware — not redistributed

This directory is a **path stub** so `projects/infineon-dc-optimizer.project.yaml`
keeps a stable `target_path`.

Infineon firmware under test is hosted on Infineon's official code portal and is
not pushed to GitHub.

AFL++ campaigns for the primary target use the sanitized harness facsimile:

`src/harness/fuzz_dc_optimizer_protocol.c`

Documentation chunks for retrieval (public only) live in:

`data/vectorstore_projects/infineon-dc-optimizer/corpus/`
